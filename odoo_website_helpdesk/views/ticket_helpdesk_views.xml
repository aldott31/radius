<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--    Form view-->
    <record id="helpdesk_ticket_view_quick_create_form" model="ir.ui.view">
        <field name="name">ticket.helpdesk.quick.view.form</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field name="subject" string="Subject"/>
                    <field name="customer_id" string="Customer"/>
                    <field name="team_id" string="Helpdesk Team" required="1"/>
                    <field name="description" string="Description"/>
                </group>
            </form>
        </field>
    </record>
    <record id="ticket_helpdesk_view_form" model="ir.ui.view">
        <field name="name">ticket.helpdesk.view.form</field>
        <field name="model">ticket.helpdesk</field>
        <field name="priority">1</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_send_reply" string="Reply"
                            type="object" class="btn-primary"/>
                    <button name="action_create_tasks" string="Create Task"
                            type="object" class="btn-primary"
                            invisible="create_task==False"/>
                    <button name="action_create_invoice" string="Create Invoice"
                            type="object" class="btn-primary"
                            invisible="billable==False"/>
                    <field name="stage_id" widget="statusbar"
                           options="{'clickable': '1'}"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button class="oe_stat_button" name="action_open_tasks"
                                string="Tasks" type="object" icon="fa-tasks"
                                invisible="create_task==False">
                        </button>
                        <button class="oe_stat_button"
                                name="action_open_merged_tickets"
                                string="Merged Tickets" type="object"
                                icon="fa-list"
                                invisible="merge_ticket_invisible==False">
                            <div class="o_stat_info">
                                <field name="merge_count" readonly="True"/>
                                <span class="o_stat_text">Merged Tickets</span>
                            </div>
                        </button>
                        <button class="oe_stat_button" name="action_open_invoices"
                                string="Invoices" type="object" icon="fa-tasks"
                                invisible="billable==False">
                        </button>
                    </div>
                    <field name="kanban_state" widget="state_selection"/>
                    <div class="oe_title">
                        <label for="name" class="oe_edit_only"/>
                        <h1>
                            <field name="name"/>
                        </h1>
                        <label for="subject" class="oe_edit_only"/>
                        <h1 class="mt0">
                            <field name="subject"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="customer_id"/>
                            <field name="ticket_type_id"/>
                            <field name="email"/>
                            <field name="phone"/>
                            <field name="tags_ids" widget="many2many_tags"/>
                        </group>
                        <group>
                            <field name="show_category" invisible="1"/>
                            <field name="priority" widget="priority"/>
                            <field name="create_date"/>
                            <field name="start_date"/>
                            <field name="end_date"/>
                            <field name="product_ids" widget="many2many_tags"/>
                            <field name="show_category" invisible="1"/>
                            <field name="merge_count" invisible="1"/>
                            <field name="merge_ticket_invisible" invisible="1"/>
                        </group>
                    </group>
                    <notebook>
                        <page string="Issue Description">
                            <field name="description"
                                   placeholder="Provide the reason in detail"/>
                        </page>
                        <page string="Attachments">
                            <field name="attachment_ids">
                                <list string="Attachments" no_open="1">
                                    <field name="name"/>
                                    <field name="res_model"/>
                                    <field name="res_field"/>
                                    <field name="res_id"/>
                                    <field name="type"/>
                                    <field name="file_size"/>
                                    <field name="company_id"/>
                                    <field name="create_uid"/>
                                    <field name="create_date"/>
                                    <field name="datas" widget="binary"
                                           filename="name"/>
                                </list>
                            </field>
                        </page>
                        <page string="Customer Rating">
                            <group>
                                <field name="customer_rating"
                                       widget="priority"/>
                                <field name="review"/>
                            </group>
                        </page>
                        <page string="Other Information">
                            <group>
                                <group>
                                    <field name="team_id"/>
                                    <field name="team_head_id"/>
                                    <field name="assigned_user_id"/>
                                    <field name="category_id"
                                           groups="odoo_website_helpdesk.group_show_category"/>
                                </group>
                                <group>
                                    <field name="project_id"/>
                                    <field name="service_product_id" invisible="create_task==False"
                                           required="create_task==True"/>
                                    <field name="show_create_task"
                                           invisible="1"/>
                                    <field name="create_task"
                                           invisible="show_create_task==False"/>
                                    <field name="billable"
                                           invisible="create_task==False"/>
                                    <field name="last_update_date"/>
                                    <field name="replied_date"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>
    <!--Tree view-->
    <record id="ticket_helpdesk_view_tree" model="ir.ui.view">
        <field name="name">ticket.helpdesk.view.tree</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="customer_id"/>
                <field name="customer_name"/>
                <field name="subject"/>
                <field name="stage_id"/>
            </list>
        </field>
    </record>
    <!--    Kanban view-->
    <record id="ticket_helpdesk_view_kanban" model="ir.ui.view">
        <field name="name">ticket.helpdesk.view.kanban</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <kanban default_group_by="stage_id" class="o_kanban_small_column"
                    on_create="quick_create"
                    quick_create_view="odoo_website_helpdesk.helpdesk_ticket_view_quick_create_form">
                <field name="color"/>
                <field name="name"/>
                <field name="subject"/>
                <field name="stage_id"/>
                <field name="kanban_state"/>
                <field name="customer_id"/>
                <field name="customer_radius_username"/>
                <field name="team_id"/>
                <field name="priority"/>
                <field name="user_id"/>
                <progressbar field="kanban_state"
                             colors="{&quot;done&quot;: &quot;success&quot;, &quot;blocked&quot;: &quot;danger&quot;, &quot;normal&quot;: &quot;200&quot;}"/>
                 <templates>
                      <t t-name="kanban-menu">
                          <t t-if="widget.editable">
                              <a role="menuitem" type="edit"
                                 class="dropdown-item">Edit
                              </a>
                          </t>
                          <t t-if="widget.deletable">
                              <a role="menuitem" type="delete"
                                 class="dropdown-item">Delete
                              </a>
                          </t>
                          <ul class="oe_kanban_colorpicker"
                              data-field="color"/>
                      </t>
                    <t t-name="kanban-box">
                        <div t-attf-class="#{!selection_mode ? kanban_color(record.color.raw_value) : ''} oe_kanban_global_click oe_kanban_card d-flex flex-column">
                            <div class="o_kanban_card_header">
                                <div class="oe_kanban_content" style="padding: 10px;">
                                    <!-- Subject (Bold & Larger) -->
                                    <div class="row mb-2">
                                        <div class="col-12">
                                            <strong style="font-size: 15px; color: #2c3e50; line-height: 1.4;">
                                                <field name="subject"/>
                                            </strong>
                                        </div>
                                    </div>
                                    
                                    <!-- Customer Name -->
                                    <div class="mb-1" style="font-size: 13px; color: #555;">
                                        <i class="fa fa-user" style="margin-right: 6px; color: #3498db; width: 14px;"/>
                                        <field name="customer_id"/>
                                    </div>
                                    
                                    <!-- RADIUS Username -->
                                    <div class="mb-1" t-if="record.customer_radius_username.raw_value" 
                                         style="font-size: 12px; color: #666;">
                                        <i class="fa fa-wifi" style="margin-right: 6px; color: #9b59b6; width: 14px;"/>
                                        <span style="font-weight: 500;">RADIUS:</span> 
                                        <field name="customer_radius_username"/>
                                    </div>
                                    
                                    <!-- Team -->
                                    <div class="mb-2" t-if="record.team_id.raw_value" 
                                         style="font-size: 12px; color: #666;">
                                        <i class="fa fa-users" style="margin-right: 6px; color: #e67e22; width: 14px;"/>
                                        <field name="team_id"/>
                                    </div>
                                    
                                    <!-- Bottom section: Priority Widget & Kanban State & User Avatar -->
                                    <div class="o_kanban_record_bottom d-flex align-items-center justify-content-between mt-2" 
                                         style="border-top: 1px solid #e8e8e8; padding-top: 10px;">
                                        <div class="d-flex align-items-center">
                                            <!-- Priority Widget (same as form view) -->
                                            <field name="priority" widget="priority"/>
                                        </div>
                                        
                                        <div class="d-flex align-items-center">
                                            <!-- Kanban State -->
                                            <field name="kanban_state" widget="state_selection" style="margin-right: 8px;"/>
                                            <!-- User Avatar -->
                                            <field name="user_id" widget="many2one_avatar_user"/>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    <!--    Activity view-->
    <record id="helpdesk_ticket_view_activity" model="ir.ui.view">
        <field name="name">ticket.helpdesk.view.activity</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <activity string="Tickets">
                <templates>
                    <div t-name="activity-box">
                        <div>
                            <field name="name" display="full"/>
                            <field name="customer_id" muted="1"
                                   display="full"/>
                        </div>
                    </div>
                </templates>
            </activity>
        </field>
    </record>
    <!--    Calendar view-->
    <record id="ticket_helpdesk_view_calendar" model="ir.ui.view">
        <field name="name">ticket.helpdesk.view.calendar</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <calendar string="Helpdesk Ticket" date_start="create_date" date_stop="end_date"
                      event_open_popup="true" mode="month"
                      color="customer_id" quick_create="0"
            >
                <field name="customer_id"/>
                <field name="subject"/>
                <field name="team_id"/>
            </calendar>
        </field>
    </record>

    <!--Pivot view-->
    <record id="help_ticket_view_pivot" model="ir.ui.view">
        <field name="name">ticket.helpdesk.pivot</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <pivot>
                <field name="customer_id" type="col"/>
                <field name="subject" type="row"/>
                <field name="team_id" type="col"/>
                <field name="customer_name" type="col"/>
                <field name="email"/>
                <field name="phone"/>
                <field name="start_date"/>
                <field name="team_id" type="col"/>
                <field name="project_id"/>
            </pivot>
        </field>
    </record>
    <!--Graph view-->
    <record id="help_ticket_view_graph" model="ir.ui.view">
        <field name="name">ticket.helpdesk.graph</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <graph>
                <field name="customer_id"/>
                <field name="subject"/>
                <field name="team_id"/>
                <field name="customer_name"/>
                <field name="email"/>
                <field name="phone"/>
                <field name="start_date"/>
                <field name="team_id"/>
                <field name="project_id"/>
            </graph>
        </field>
    </record>
    <!--Search view-->
    <record id="help_ticket_search" model="ir.ui.view">
        <field name="name">ticket.helpdesk.search</field>
        <field name="model">ticket.helpdesk</field>
        <field name="arch" type="xml">
            <search string="Ticket Search">
                <field name="customer_id" string="Customer"/>
                <field name="customer_radius_username" string="RADIUS Username"/>
                
                <!-- Filtrat ekzistues -->
                <filter string="my_ticket" name="My Ticket"
                        domain="[('assigned_user_id', '=', uid)]"/>
                <filter string="Archived" name="filter_archived"
                        domain="[('active','=', False)]"/>
                <filter string="UnArchived" name="filter_unarchived"
                        domain="[('active','=', True)]"/>
                
                <!-- Filtra sipas Priority (0-3) -->
                <separator/>
                <filter string="Very Low Priority" name="priority_very_low"
                        domain="[('priority', '=', '0')]"/>
                <filter string="Low Priority" name="priority_low"
                        domain="[('priority', '=', '1')]"/>
                <filter string="Normal Priority" name="priority_normal"
                        domain="[('priority', '=', '2')]"/>
                <filter string="High Priority" name="priority_high"
                        domain="[('priority', '=', '3')]"/>
                
                <!-- Filtra sipas Rating (0-3) -->
                <separator/>
                <filter string="Very Low Rating" name="rating_very_low"
                        domain="[('customer_rating', '=', '0')]"/>
                <filter string="Low Rating" name="rating_low"
                        domain="[('customer_rating', '=', '1')]"/>
                <filter string="Normal Rating" name="rating_normal"
                        domain="[('customer_rating', '=', '2')]"/>
                <filter string="High Rating" name="rating_high"
                        domain="[('customer_rating', '=', '3')]"/>
                
                <!-- Filtra sipas kanban state -->
                <separator/>
                <filter string="Ready" name="state_normal"
                        domain="[('kanban_state', '=', 'normal')]"/>
                <filter string="In Progress" name="state_done"
                        domain="[('kanban_state', '=', 'done')]"/>
                <filter string="Blocked" name="state_blocked"
                        domain="[('kanban_state', '=', 'blocked')]"/>
                
                <!-- Group By -->
                <group expand="0" string="Group By">
                    <filter name="Subject" string="Subject"
                            context="{'group_by':'subject'}"/>
                    <filter name="Customer" string="Customer"
                            context="{'group_by':'customer_id'}"/>
                    <filter name="Team" string="Team"
                            context="{'group_by':'team_id'}"/>
                    <filter name="Team_Head" string="Team Head"
                            context="{'group_by':'team_head_id'}"/>
                    <filter name="assign" string="Assigned To"
                            context="{'group_by':'assigned_user_id'}"/>
                    <filter name="Priority" string="Priority Level"
                            context="{'group_by':'priority'}"/>
                    <filter name="Rating" string="Rating"
                            context="{'group_by':'customer_rating'}"/>
                    <filter name="Stage" string="Stage"
                            context="{'group_by':'stage_id'}"/>
                    <filter name="Kanban_State" string="Kanban State"
                            context="{'group_by':'kanban_state'}"/>
                </group>
            </search>
        </field>
    </record>

    <!--    Action for ticket helpdesk model.-->
    <record id="ticket_helpdesk_action_report" model="ir.actions.act_window">
        <field name="name">Report Help Desk</field>
        <field name="res_model">ticket.helpdesk</field>
        <field name="view_mode">graph,search</field>
    </record>
    <!-- Define the action for the general Helpdesk view -->
    <record id="ticket_helpdesk_action" model="ir.actions.act_window">
        <field name="name">Helpdesk</field>
        <field name="res_model">ticket.helpdesk</field>
        <field name="view_mode">kanban,list,form,calendar,activity,pivot,graph</field>
    </record>

    <!-- Define the action for the My Helpdesk view with a specific domain -->
    <record id="ticket_helpdesk_action_my" model="ir.actions.act_window">
        <field name="name">My Helpdesk</field>
        <field name="res_model">ticket.helpdesk</field>
        <field name="domain">['|', '|', '|', ('assigned_user_id','=',uid), ('team_id.team_lead_id','=', uid), ('team_id.member_ids','in', [uid]), ('is_finance_visible','=', True)]</field>
        <field name="view_mode">kanban,list,form,calendar,activity,pivot,graph</field>
    </record>
</odoo>