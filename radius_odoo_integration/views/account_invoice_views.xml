<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add "Kontrate e Re" button to Invoice form header -->
    <record id="view_account_invoice_form_contract_button" model="ir.ui.view">
        <field name="name">account.move.form.contract.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">
            <xpath expr="//header/button[@name='action_post']" position="after">
                <button name="%(radius_odoo_integration.action_contract_wizard)d"
                        string="📄 Kontrate e Re"
                        type="action"
                        class="btn-success"
                        context="{'default_partner_id': partner_id}"
                        invisible="move_type != 'out_invoice' or partner_has_contract or payment_state not in ['paid', 'in_payment']"
                        groups="asr_radius_manager.group_isp_sales,asr_radius_manager.group_isp_manager"/>
            </xpath>
        </field>
    </record>

</odoo>
